# OS files
.DS_Store
Thumbs.db

# Do NOT ignore generated repo files — they MUST be committed
# so Mihon can download them via raw.githubusercontent.com.
# The index files and APKs are the entire point of this repo.
